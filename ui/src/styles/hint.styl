@import 'common'

// Field hint
.hint {
  font-size: 12px;
  color: $theme-black-3;
  padding: 5px 0;
  float: left;
  max-width: 99%;

  &_error {
    color: $failed;
  }

  &_right {
    float: right;
    color: $theme-primary-color;
    cursor: pointer;
  }
}
